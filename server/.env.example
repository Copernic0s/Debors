# Copy this file to "server/.env" (do NOT commit server/.env).
# The server also loads "../.env" automatically, so you can keep everything in one .env if you prefer.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
CMP_INGEST_SECRET=

